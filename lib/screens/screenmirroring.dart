import 'package:flutter/material.dart';

class ScreenMirroringPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Screen Mirroring'),
      ),
      body: Center(
        child: Text('Screen Mirroring Page'),
      ),
    );
  }
}
