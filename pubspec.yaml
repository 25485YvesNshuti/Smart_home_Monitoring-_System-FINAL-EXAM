name: ndobasmarthomesensormobileapp
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.3.2 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  cupertino_icons: ^1.0.6
  lottie: ^3.1.0
  provider: ^6.1.1
  shared_preferences: ^2.0.12
  google_maps_flutter: ^2.2.8
  flutter_polyline_points: ^2.0.0
  geolocator: ^11.0.0
  location: ^5.0.3
  proximity_sensor: ^1.0.4
  sensors_plus: ^5.0.0  
  flutter_speed_dial: ^7.0.0
  light_sensor: ^3.0.0
  flutter_local_notifications: ^17.1.2
  charts_flutter: ^0.12.0
  font_awesome_flutter: ^10.0.0 # Updated dependency
  # Added dependency for screen brightness

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - lib/assets/Animation - 1712058538433.json
    - lib/assets/Animation - 1712341175314.json
    - lib/assets/compass-arrow-navigation.png
    - lib/assets/bulb.png
    - lib/assets/desktop-wallpaper-iot-smart-home.png
    - lib/assets/smart_home.png